`list.json` is hand-authored from the documented `podman-machine-list(1)` JSON schema, not captured from a real `podman machine list --format json` run.
`podman` is not installed on this repo's build host (Docker Desktop via WSL only) — see the brief in the containers-plan.md task list.
