Hand-authored canned output for `stub_engine`'s `compose ... config
--services` answer, used by the `e2e_containers` E2E flow
(`crates/app/tests/e2e_containers.rs`) against
`crates/app/tests/fixtures/containers/compose.yml`'s two services
(`web`, `db`).
